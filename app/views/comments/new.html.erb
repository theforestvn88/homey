<%= render(layout: 'shared/modal', locals: { title: 'New comment' }) do %>
  <div class="md:w-2/3 w-full">
    <h1 class="font-bold text-4xl">New comment</h1>

    <%= render "form", comment: @comment %>
  </div>
<% end %>
